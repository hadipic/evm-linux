## progress

进度条，用于显示内容加载或操作处理进度。

#### 子组件
不支持。

#### 属性
除支持组件通用属性外，还支持如下属性：

| 名称 | 类型 | 描述 |
| :-----| ----: | ---- |
| percent | string | 当前进度。取值范围为0-100。 |

#### 样式
支持组件通用样式。

#### 例子
<a href="../../tutorial/evue/progress.evue">progress.evue</a>