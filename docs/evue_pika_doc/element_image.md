## image

图片组件，用来渲染展示图片。

#### 子组件
不支持。

#### 属性
除支持组件通用属性外，还支持如下属性：

| 名称 | 类型 | 描述 |
| :-----| ----: | ---- |
| src | string | 图片的路径，支持的图片格式包括png、lvgl bin。 |

#### 样式
支持组件通用样式。

#### 例子
<a href="../../tutorial/evue/image.evue">image.evue</a>